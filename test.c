#include <stdio.h>

int main(int argc, char const *argv[])
{
	/*int two = 2;
	float a = (double)two;

	float decimal = 5.2;
	int s = (int) decimal;
	printf("%d\n",s );
	//float a = 3 / (two+0.0);
	printf("%1.1f\n",a ); */
	int free = 3;
	int two = 2;
	int f = 4;
	printf("%1.1f\n", two+0.0 );//to type int add type float 2+0.0=2.00
	printf("%1.1f\n", (double)free);//yavnoe izmeneniye
	int index = (f/(double)two);
	printf("%d\n",index );
	
}